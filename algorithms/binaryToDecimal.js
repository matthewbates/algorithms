// complete the function which converts a binary number, given as a string, to a decimal

function binToDec(bin) {
  // Number used in lieu of parseInt()
  let digit = Number(bin, 2);
  return digit;
}
